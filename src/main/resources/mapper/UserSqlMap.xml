<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hz.springboot.dao.SpringBootDao">

    <select id="getUserById" parameterType="java.lang.Integer" resultType="com.hz.springboot.domain.User">
        SELECT id, `name`, content FROM hz_test.test1 WHERE id = #{userId};
    </select>
</mapper>